---
import FundingPopup from "./FundingPopup.astro";
---

<section id="pricing" class="relative isolate bg-zinc-50 py-16 sm:py-20">
  <div class="mx-auto max-w-4xl px-6 lg:px-8">
    <!-- Heading -->
    <div class="text-center reveal-up" style="transition-delay:40ms">
      <h2 class="text-3xl font-extrabold tracking-tight text-zinc-900 sm:text-4xl">Ile kosztują złe decyzje architektoniczne?</h2>
    </div>

    <!-- Value justification -->
    <div class="mx-auto mt-8 max-w-3xl space-y-4 text-zinc-800">
      <p class="reveal-up" style="transition-delay:80ms">Jedna źle podjęta decyzja może kosztować firmę 
          <span class="font-semibold">setki tysięcy złotych </span> w dodatkowych kosztach utrzymania i przebudowy systemu.</p>
      <p class="reveal-up" style="transition-delay:100ms">Ten kurs kosztuje <span class="font-semibold">2100 PLN</span> - mniej niż 1 dzień pracy zespołu, a wiedza zostaje z Tobą na lata.</p>
    </div>

    <!-- Funding highlight (moved up and emphasized) -->
    <div class="mx-auto mt-6 max-w-3xl">
      <div class="funding-animate relative flex items-start gap-3 rounded-2xl bg-gradient-to-r from-emerald-600 via-emerald-500 to-teal-500 p-4 sm:p-5 text-white shadow-lg ring-1 ring-inset ring-white/15 reveal-up" style="transition-delay:140ms">
        <span class="icon-pulse inline-flex h-9 min-w-9 items-center justify-center rounded-lg bg-white/15 text-white shadow-sm backdrop-blur-sm">
          <svg viewBox="0 0 24 24" class="h-5 w-5" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M19 5L5 19"/>
            <circle cx="7" cy="7" r="2"/>
            <circle cx="17" cy="17" r="2"/>
          </svg>
        </span>
        <div class="flex-1">
          <p class="text-base sm:text-lg leading-snug">
            <span class="mr-2 inline-flex items-center gap-2 rounded-full bg-white/15 px-2.5 py-1 text-[11px] font-semibold uppercase tracking-wide ring-1 ring-inset ring-white/20">Dofinansowanie</span>
            <span class="font-bold">Do <span class="percent-pulse">70%</span> kosztów szkolenia</span>
          </p>
          <p class="mt-1 text-sm/6 text-white/90">Skontaktuj się z nami, aby poznać szczegóły i sprawdzić kwalifikację.</p>
        </div>
        <a href="#funding" data-open-funding class="ml-auto inline-flex items-center rounded-md bg-white/90 px-3 py-2 text-sm font-semibold text-emerald-700 shadow-sm hover:bg-white focus:outline-none focus:ring-2 focus:ring-white/30 transition" aria-label="Zgłoś zainteresowanie dofinansowaniem">
          Pobierz szczegółowe informacje
        </a>
        <span class="absolute -top-1.5 -right-1.5 h-3 w-3 rounded-full bg-white/70 shadow ring-2 ring-white/40 animate-ping [animation-duration:1.6s]"></span>
        <span class="absolute -top-1.5 -right-1.5 h-3 w-3 rounded-full bg-white/90 shadow ring-2 ring-white/40"></span>
      </div>
    </div>

    <!-- Options -->
    <div class="mx-auto mt-10 grid max-w-3xl grid-cols-1 gap-4 sm:grid-cols-2">
      <!-- Individual -->
      <div class="rounded-xl border border-zinc-200 bg-white p-6 shadow-sm reveal-up" style="transition-delay:120ms">
        <h3 class="text-lg font-semibold text-zinc-900">Bilet indywidualny</h3>
        <p class="mt-1 text-zinc-600">Dla jednej osoby</p>
        <p class="mt-4 text-3xl font-extrabold text-zinc-900">2100 PLN</p>
      </div>
      <!-- Company -->
      <div class="rounded-xl border border-zinc-200 bg-white p-6 shadow-sm reveal-up" style="transition-delay:140ms">
        <h3 class="text-lg font-semibold text-zinc-900">Bilet firmowy</h3>
        <p class="mt-1 text-zinc-600">Dla zespołów 3+ osób</p>
        <p class="mt-4 text-3xl font-extrabold text-zinc-900">rabat 10%</p>
      </div>
    </div>


  </div>
</section>

<FundingPopup />
