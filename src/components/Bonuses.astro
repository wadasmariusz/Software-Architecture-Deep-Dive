<section id="bonuses" class="relative isolate bg-white py-16 sm:py-20">
  <div class="mx-auto max-w-4xl px-6 lg:px-8">
    <!-- Heading -->
    <div class="text-center reveal-up" style="transition-delay:40ms">
      <h2 class="text-3xl font-extrabold tracking-tight text-zinc-900 sm:text-4xl">Dlaczego warto zapisać się teraz</h2>
    </div>

    <!-- Bonuses / Limitations -->
    <ul class="mx-auto mt-10 max-w-3xl space-y-4 text-zinc-800">
      <li class="flex items-start gap-3 reveal-up" style="transition-delay:80ms">
        <svg viewBox="0 0 24 24" class="mt-0.5 h-5 w-5 text-amber-600 flex-shrink-0" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <circle cx="12" cy="12" r="10"/><path d="M12 8v4"/><path d="M12 16h.01"/>
        </svg>
        <span>Ograniczona liczba miejsc - kurs jest warsztatowy, a nie masowy webinar.</span>
      </li>
      <li class="flex items-start gap-3 reveal-up" style="transition-delay:100ms">
        <svg viewBox="0 0 24 24" class="mt-0.5 h-5 w-5 text-green-600 flex-shrink-0" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M20 6L9 17l-5-5"/>
        </svg>
        <span>Pierwsze osoby dostają dodatkową indywidulaną konsultację (60 minut).</span>
      </li>
      <li class="flex items-start gap-3 reveal-up" style="transition-delay:120ms">
        <svg viewBox="0 0 24 24" class="mt-0.5 h-5 w-5 text-accent flex-shrink-0" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M8 7V3h8v4"/>
          <rect x="4" y="7" width="16" height="13" rx="2" ry="2"/>
          <path d="M16 13h-8"/>
        </svg>
        <span>Zapis do <strong>30.09.2025</strong> gwarantuje bonusowe materiały + niższą cenę.</span>
      </li>
    </ul>
  </div>
</section>

